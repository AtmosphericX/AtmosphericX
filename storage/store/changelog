========= 8.0.0.033 (beta-pre-dashboard-testing) =========

fix (calculations): Distance is now float(2)
fix (configurations:jsonc) Tornado Watch (events.jsonc) entry
fix (backend:global) Update property access to use optional chaining for better safety across various modules
fix (frontend:utils) Update filtering logic to handle undefined properties gracefully
fix (frontend:utils) Improve event handling in PulsePoint updates to prevent errors
refactor (structure:register) Remove client data as that can be grabbed directly from the config endpoint.
feat (frontend:utils) Implement getEventMetadata function to centralize event metadata retrieval
